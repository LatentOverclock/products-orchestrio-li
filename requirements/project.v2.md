# project.v2 - Delta requirements

## Goal
Refine table-view behavior to support operational product management workflows.

## Functional requirements (delta)
1. The table-view must include all product properties except `description`:
   - name
   - purchase-link
   - shop-link
   - booqable-link
   - manual-link
   - inspection-link
   - status
2. The table-view must support sorting by each listed table property.
3. The table-view must support filtering by each listed table property.

## Notes
- This file contains only changes introduced after `project.v1.md`.
